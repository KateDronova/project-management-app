import { EmailExistsDirective } from './user-exists.directive';

describe('EmailExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailExistsDirective();
    expect(directive).toBeTruthy();
  });
});
