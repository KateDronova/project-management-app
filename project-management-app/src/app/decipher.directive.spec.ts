import { DecipherDirective } from './decipher.directive';

describe('DecipherDirective', () => {
  it('should create an instance', () => {
    const directive = new DecipherDirective();
    expect(directive).toBeTruthy();
  });
});
